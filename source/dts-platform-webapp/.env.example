# Example environment configuration for dts-platform-webapp
# Copy to .env.development.local or .env.production.local as needed

# Base path for API requests (same-origin calls)
VITE_API_BASE_URL=/api

# Optional proxy target used by `pnpm dev`; override per environment
# VITE_API_PROXY_TARGET=http://localhost:18082

# Feature toggles
VITE_ENABLE_MOCK=false
VITE_ENABLE_PORTAL_MENU_MGMT=false

# Branding
VITE_APP_NAME=数据管理平台
VITE_APP_VERSION=1.0.0

# Routing configuration (keep frontend+hash to match dev server behaviour)
VITE_PUBLIC_PATH=/
VITE_APP_ROUTER_MODE=frontend
VITE_APP_ROUTER_HISTORY=hash
VITE_APP_DEFAULT_ROUTE=/workbench
 
# Allowed roles to sign in (comma-separated). Empty to allow all authenticated users.
VITE_ALLOWED_LOGIN_ROLES=ROLE_USER,ROLE_OP_ADMIN,ROLE_IAM_ADMIN,ROLE_CATALOG_ADMIN,ROLE_GOV_ADMIN
